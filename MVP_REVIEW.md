# MVPレビュー（2026-04-23）

このドキュメントは、現在のPR内容を「MVP完成」と見なせるかを確認するためのレビュー記録です。

## 結論

- **MVPとしての主機能は成立**（データ取込、分析、保存、表示、通知抑制、テストデータ生成/削除）。
- ただし、運用面の最低限品質を満たすために、今回のPRで以下を修正済み:
  - CLI実行時にDBテーブル未作成で失敗する問題を解消。
  - scheduler単体起動時にもDBテーブルを自動作成するように統一。

## レビュー観点と確認結果

### 1) 方針整合（READMEとの一致）

- README記載のMVP機能（Yahoo取得、分析、DB保存、UI表示、通知）に対応する実装が存在。
- 分析は `analysis -> signal_service` 経由で実行され、結果は `analysis_results` に保存される。
- Yahoo生データは `price_bars` にUpsertされる。

### 2) 動作確認（ローカル実行）

- `pytest` が通過（テストデータ生成/削除の安全性テストを含む）。
- CLIで以下のE2E相当フローを確認:
  1. 合成データ生成
  2. 指定日分析
  3. 合成データ削除

### 3) コードレビュー観点

- **修正前課題**:
  - `python -m app.cli ...` 実行時に `symbols` テーブル未作成だと失敗。
  - `python -m app.scheduler` 単体起動時も同様に初期DBが未作成だと失敗し得る。
- **修正内容**:
  - `app/cli.py` の `main()` 冒頭で `Base.metadata.create_all(bind=engine)` を実行。
  - `app/scheduler.py` の `main()` 冒頭で同様にテーブル作成を実行。
- **効果**:
  - Webサーバ起動を経由しない運用（CLI実行、worker単独起動）でも初回起動に強くなり、
    MVP運用の導線がブレなくなった。

## 残課題（MVP後に推奨）

- 通知失敗時のリトライ/エラーログ強化。
- Yahooレート制限時の中断結果を運用者へ通知（現状は部分成功で終了）。
- 最低限の統合テスト（ingest→analyze→DB確認）をCIに追加。
